# Collage

note this doesn't resize images, so the output can get quite large, depending on the dimensions of what's added (from a folder of jpegs, the path to which needs to be pasted into "path", which is the first thing you should see when you open the file!
